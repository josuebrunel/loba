language: cpp
compiler:
   - gcc
branches:
   only:
      - master
notifications:
   email:
      - gkounkou@gmail.com
      - josuebrunel@gmail.com
   on_succes : change
   on_failure: always
os:
   - linux
before_install:
   - if [ $TRAVIS_OS_NAME == linux ]; then cmake .. && make; fi
